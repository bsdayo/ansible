prometheus.scrape "minecraft" {
	targets = [
		{"__address__" = "127.0.0.1:19565", instance = "{{ ansible_hostname }}"},
	]
	forward_to      = [prometheus.remote_write.default.receiver]
	scrape_interval = "10s"
}
